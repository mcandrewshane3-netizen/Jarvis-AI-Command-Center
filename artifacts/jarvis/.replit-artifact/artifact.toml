kind = "web"
previewPath = "/"
title = "JARVIS AI Command Center"
description = "An iPad-first personal AI command center for daily briefings, work, finances, markets, and controlled automations."
version = "1.0.0"
id = "artifacts/jarvis"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21662

[services.development]
run = "pnpm --filter @workspace/jarvis run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jarvis", "run", "build" ]
serve = "static"
publicDir = "artifacts/jarvis/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21662"
BASE_PATH = "/"
